import styled from "styled-components";

export const CartContainer = styled.div`
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 2rem;
    color: #3466AA;
    cursor: pointer;

    p {
        font-size: 1.2rem;
    }
`;